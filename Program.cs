using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Program: MonoBehaviour
{
    [SerializeField] private Node startingPoint;
    [SerializeField] private Node endingPoint;
    //Escribir el timer

    void start()
    {

    }

    //Pasar aquí la conversion de los nodos e imprimir las coordenadas del path (1,1), (1,2), etc. en un Console.Writeline

    //SearchPath.getPath(state)
}
